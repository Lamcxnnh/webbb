<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.lease.web.admin.mapper.FacilityInfoMapper">

    <select id="selectFacilityInfoByApartmentId" resultType="com.example.lease.model.entity.FacilityInfo">
        select fi.id,
               fi.type,
               fi.name,
               fi.icon
        from facility_info fi
        where fi.is_deleted = 0
          and fi.type = #{itemType}
          and
            fi.id in (select facility_id from apartment_facility where is_deleted = 0 and apartment_id = #{apartmentId})
    </select>
</mapper>
